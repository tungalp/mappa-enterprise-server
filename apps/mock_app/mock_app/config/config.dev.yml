db:
  url: "postgresql+asyncpg://postgres:postgres@localhost/mapa_test"
oidc:
  jwks_uri: "http://localhost:33100/api/sso/oidc/jwks"
elk:
  host: "host.docker.internal"
  port: 5000
apm:
  service_name: "mapa-mock_app"
  secret_token: ""
  server_url: "http://host.docker.internal:8200"
  environment: "mapa-mock_app-service-environment"
